module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
//     * Create a file named `app/globals.css` and add the following lines:
// ```css
// @tailwind base;
// @tailwind components;
// @tailwind utilities;
//     * Ensure your `app/layout.jsx` (which Next.js creates by default) imports this CSS file.